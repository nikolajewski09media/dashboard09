---
import TableMui from "./react/TableMui/Table.jsx";
import { dates, getDataInRange } from "../utils/dataStore.js";

// Verwendung der Funktion mit den ursprünglichen Werten
const $dates = dates.get();

const initalData = await getDataInRange($dates);
---

<TableMui {initalData} client:load />
