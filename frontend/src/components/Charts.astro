---
import { getDataInRangeNew } from "../utils/dataStore";
import ChartsReact from "./react/Charts.jsx";
import { dates, $fetchedData } from "../utils/dataStore";
const $dates = dates.get();
const data = $fetchedData.get();
const initialData = await getDataInRangeNew(data, $dates);
---

<ChartsReact {initialData} client:load />
